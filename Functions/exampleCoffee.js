//const and the => at the end creates function which is in yellow size and coffee type get made in to varibles when they are in brackets//
const coffeeOrder = (size, coffeeType) => {
    console.log(`Here is your ${size} ${coffeeType}`)
}

coffeeOrder("Big","Hot Chololate")
